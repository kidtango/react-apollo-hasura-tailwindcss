import {
  authClientId,
  authDomain,
  callbackUrl
} from '../../components/utils/constants';

export const AUTH_CONFIG = {
  domain: authDomain,
  clientId: authClientId,
  callbackUrl: callbackUrl
};
